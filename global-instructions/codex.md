## Skill catalog

Before loading specialized skills, run:

```bash
skill-catalog prompt "<the user's task>"
```

Read only the selected `SKILL.md` files. For ambiguous requests, use
`--backend opencode`. The catalog contains metadata and paths; selected skill
files remain authoritative.
